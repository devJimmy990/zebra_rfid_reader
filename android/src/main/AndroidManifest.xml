<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.jimmy.rfid_zebra_reader">
    
    <!-- Bluetooth permissions for RFID reader connection -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    
    <!-- Location required for Bluetooth scanning on Android -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- For USB connection (if needed) -->
    <uses-permission android:name="android.permission.USB_PERMISSION" />
    
    
    <!-- Empty application tag to prevent conflicts -->
    <application tools:node="merge" />
</manifest>